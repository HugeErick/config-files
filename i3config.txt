set $mod Mod1
font pango:BitstromWera Nerd Font Mono 12

## Autostart ##
exec --no-startup-id pipewire
exec --no-startup-id nm-applet
exec --no-startup-id blueman-applet
exec --no-startup-id nohup greenclip daemon &
exec --no-startup-id flameshot
exec --no-startup-id picom --config ~/.config/picom/picom.conf &

## audio / brightness ## 
set $refresh_i3status killall -SIGUSR1 i3status
bindsym XF86AudioRaiseVolume  exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ +10% && $refresh_i3status
bindsym XF86AudioLowerVolume  exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ -10% && $refresh_i3status
bindsym XF86AudioMute         exec --no-startup-id pactl set-sink-mute @DEFAULT_SINK@ toggle && $refresh_i3status
bindsym XF86AudioMicMute      exec --no-startup-id pactl set-source-mute @DEFAULT_SOURCE@ toggle && $refresh_i3status
bindsym XF86MonBrightnessUp   exec --no-startup-id brightnessctl set +10%
bindsym XF86MonBrightnessDown exec --no-startup-id brightnessctl set 10%-

## Mouse / Drag ##
floating_modifier $mod
tiling_drag modifier titlebar

## Core Binds ##
bindsym $mod+t exec alacritty
bindsym $mod+Shift+q kill
bindsym $mod+d exec --no-startup-id dmenu_run

## Custom Binds ##
bindsym $mod+Tab exec --no-startup-id i3-msg workspace next
bindsym $mod+Shift+Tab exec --no-startup-id i3-msg workspace prev

bindsym $mod+n exec --no-startup-id rofi -show run
bindsym Print exec flameshot gui
exec --no-startup-id greenclip daemon>/dev/null
bindsym $mod+v exec --no-startup-id rofi -modi "clipboard:greenclip print" -show clipboard
bindsym $mod+z exec --no-startup-id ~/.local/bin/boomer

## Window Assignments ##
assign [class="(?i)firefox"] $ws2
assign [class="Chromium"]    $ws2
assign [class="Spotify"]     $ws6
assign [class="discord"]     $ws5
assign [class="obsidian"]    $ws3
assign [class="Windsurf"]    $ws3

## Focus ##
bindsym $mod+j         focus left
bindsym $mod+k         focus down
bindsym $mod+l         focus up
bindsym $mod+semicolon focus right
bindsym $mod+Left      focus left
bindsym $mod+Down      focus down
bindsym $mod+Up        focus up
bindsym $mod+Right     focus right

## Move ##
bindsym $mod+Shift+j         move left
bindsym $mod+Shift+k         move down
bindsym $mod+Shift+l         move up
bindsym $mod+Shift+semicolon move right
bindsym $mod+Shift+Left      move left
bindsym $mod+Shift+Down      move down
bindsym $mod+Shift+Up        move up
bindsym $mod+Shift+Right     move right

## Layout ##
bindsym $mod+h           split h
bindsym $mod+b           split v
bindsym $mod+f           fullscreen toggle
bindsym $mod+s           layout stacking
bindsym $mod+w           layout tabbed
bindsym $mod+e           layout toggle split
bindsym $mod+Shift+space floating toggle
bindsym $mod+space       focus mode_toggle
bindsym $mod+a           focus parent

## Workspaces ##
set $ws1  "1"
set $ws2  "2"
set $ws3  "3"
set $ws4  "4"
set $ws5  "5"
set $ws6  "6"
set $ws7  "7"
set $ws8  "8"
set $ws9  "9"
set $ws10 "10"

bindsym $mod+1 workspace number $ws1
bindsym $mod+2 workspace number $ws2
bindsym $mod+3 workspace number $ws3
bindsym $mod+4 workspace number $ws4
bindsym $mod+5 workspace number $ws5
bindsym $mod+6 workspace number $ws6
bindsym $mod+7 workspace number $ws7
bindsym $mod+8 workspace number $ws8
bindsym $mod+9 workspace number $ws9
bindsym $mod+0 workspace number $ws10

bindsym $mod+Shift+1 move container to workspace number $ws1
bindsym $mod+Shift+2 move container to workspace number $ws2
bindsym $mod+Shift+3 move container to workspace number $ws3
bindsym $mod+Shift+4 move container to workspace number $ws4
bindsym $mod+Shift+5 move container to workspace number $ws5
bindsym $mod+Shift+6 move container to workspace number $ws6
bindsym $mod+Shift+7 move container to workspace number $ws7
bindsym $mod+Shift+8 move container to workspace number $ws8
bindsym $mod+Shift+9 move container to workspace number $ws9
bindsym $mod+Shift+0 move container to workspace number $ws10

## i3 Control ##
bindsym $mod+Shift+c reload
bindsym $mod+Shift+r restart
bindsym $mod+Shift+e exec "i3-nagbar -t warning -m '👁  Exit i3? This will end your X session.' -B 'Yes, exit' 'i3-msg exit'"

## Resize Mode ##
mode "resize" {
    bindsym j         resize shrink width  10 px or 10 ppt
    bindsym k         resize grow   height 10 px or 10 ppt
    bindsym l         resize shrink height 10 px or 10 ppt
    bindsym semicolon resize grow   width  10 px or 10 ppt
    bindsym Left      resize shrink width  10 px or 10 ppt
    bindsym Down      resize grow   height 10 px or 10 ppt
    bindsym Up        resize shrink height 10 px or 10 ppt
    bindsym Right     resize grow   width  10 px or 10 ppt
    bindsym Return    mode "default"
    bindsym Escape    mode "default"
    bindsym $mod+r    mode "default"
}
bindsym $mod+r mode "resize"

## eye of amber theme ##

#  Palette:
#    Sclera (bg)       #0d0b0a  — near-black warm dark
#    Iris deep         #1a1200  — very dark amber ground
#    Iris mid          #c87f00  — amber / burnt gold
#    Iris bright       #ffb300  — glowing amber highlight
#    Pupil             #000000  — pure black
#    Slit (focus glow) #ffd54f  — warm pale gold
#    Dim               #4a3800  — muted iris for inactive
#    Text light        #fff8e1  — warm off-white
#    Text dim          #8a7040  — dimmed text
#    Urgent            #bf360c  — deep ember red

## Gaps (the "iris ring" spacing) ## 
gaps inner 8
gaps outer 4
smart_gaps on

## Window Borders ##
default_border          pixel 2
default_floating_border pixel 2
hide_edge_borders       smart

## Color Definitions ##
# class                   border    bg        text      indicator  child_border
client.focused            #ffb300   #1a1200   #fff8e1   #ffd54f    #ffb300
client.focused_inactive   #4a3800   #0d0b0a   #8a7040   #4a3800    #4a3800
client.unfocused          #1a1200   #0d0b0a   #4a3800   #1a1200    #1a1200
client.urgent             #bf360c   #3b0a00   #fff8e1   #bf360c    #bf360c
client.placeholder        #0d0b0a   #0d0b0a   #4a3800   #0d0b0a    #0d0b0a
client.background         #0d0b0a

## i3bar ##
bar {
    status_command    i3status --config ~/.config/i3status/config
    position          bottom
    font              pango:BitstromWera Nerd Font Mono 10

    # transparency requires a compositor (picom)
    i3bar_command     i3bar --transparency

    colors {
        # bar background — semi-transparent iris dark
        background        #0d0b0acc
        statusline        #ffb300
        separator         #4a3800

        # workspace buttons:  border    bg        text
        focused_workspace   #ffb300   #1a1200   #fff8e1
        active_workspace    #4a3800   #0d0b0a   #c87f00
        inactive_workspace  #1a1200   #0d0b0acc #4a3800
        urgent_workspace    #bf360c   #3b0a00   #fff8e1
        binding_mode        #ffd54f   #1a1200   #0d0b0a
    }
}

## Hardware ##
exec --no-startup-id xinput set-prop 9 351 1 1 1 1 1 1 1
exec --no-startup-id xinput set-prop 9 344 -83 -83
